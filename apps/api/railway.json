{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm --filter @opfun/shared build && pnpm --filter @opfun/opnet build && pnpm --filter api run db:generate && pnpm --filter api run build"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
